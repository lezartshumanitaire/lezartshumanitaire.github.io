source 'https://rubygems.org'

# you can test github pages with a given dependency version
# you can find useful informations here https://rubygems.org/gems/github-pages/
#gem 'github-pages', '43'

# playing with github pages current version
# no need to add any of the gh-pages accepted plugins (like jekyll-paginate) here
# they are already installed by gh-pages plugin
# gem 'github-pages'

# playing with the last jekyll's version
gem 'jekyll'

# playing with a specific jekyll's version
# gem 'jekyll', '3.1.6'

# here you need to explicitelly call your jekyll plugins like, ...
# gem 'jekyll-paginate'

# gem 'jekyll-responsive_image'
